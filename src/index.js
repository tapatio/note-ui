// Entry point to react.

import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// Render react component into the element with id root (<div> in index.html).
ReactDOM.render(<App />, document.getElementById('root'));
